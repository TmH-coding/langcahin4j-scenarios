# RAG 核心概念深度讲解

## 第一章：向量化（Embedding）的本质

### 1.1 为什么需要向量？

**问题：** 计算机如何理解文本的含义？

```
文本：     "我想退货"
计算机看到的：  [字符 '我', 字符 '想', 字符 '退', 字符 '货']
计算机理解的：  ??? （无法理解含义）
```

**解决方案：** 将文本转换为数字向量

```
文本：     "我想退货"
向量：    [0.123, -0.456, 0.789, ..., 0.234]  (384 维)
含义：    这个向量代表"退货"这个概念
```

### 1.2 向量的几何意义

**关键概念：** 向量空间中，相似的文本距离近，不同的文本距离远

```
向量空间示意（简化为 2D）：

        ↑ 维度 2
        |
    "退货"  ●
        |  \
        |   \ 距离近（相似）
        |    \
    "退款"  ●
        |
        |
    "产品质量" ●
        |
        └─────────→ 维度 1

"退货" 和 "退款" 距离近 → 相似度高
"退货" 和 "产品质量" 距离远 → 相似度低
```

### 1.3 Embedding 模型的工作原理

**AllMiniLmL6V2 模型（Scenario-2 使用）：**

```
输入文本：  "我想退货"
    ↓
分词：      ["我", "想", "退", "货"]
    ↓
词向量：    [
              [0.1, 0.2, 0.3, ...],  // "我" 的向量
              [0.4, 0.5, 0.6, ...],  // "想" 的向量
              [0.7, 0.8, 0.9, ...],  // "退" 的向量
              [0.2, 0.3, 0.4, ...]   // "货" 的向量
            ]
    ↓
聚合（Pooling）：取平均值或最大值
    ↓
输出向量：  [0.35, 0.45, 0.55, ..., 0.25]  (384 维)
```

**关键点：**
- 输入可以是任意长度的文本
- 输出总是固定维度（384 维）
- 相同的输入总是产生相同的向量
- 相似的输入产生相似的向量

### 1.4 向量维度的含义

**384 维是什么意思？**

```
每一维代表文本的一个特征：

维度 1：  是否关于"退货"？ (0.8 = 很可能)
维度 2：  是否关于"金钱"？ (0.6 = 可能)
维度 3：  是否关于"时间"？ (0.2 = 不太可能)
维度 4：  是否关于"产品"？ (0.7 = 很可能)
...
维度 384：其他特征

向量 = [0.8, 0.6, 0.2, 0.7, ..., 0.5]
```

**不同模型的维度：**
- AllMiniLmL6V2：384 维（轻量级）
- OpenAI text-embedding-3-small：1536 维（中等）
- OpenAI text-embedding-3-large：3072 维（强大）

**维度越高 = 表达能力越强，但计算成本越高**

---

## 第二章：相似度计算的数学原理

### 2.1 余弦相似度（Cosine Similarity）

**定义：** 两个向量夹角的余弦值

```
公式：
cos(A, B) = (A · B) / (|A| × |B|)

其中：
- A · B 是点积（dot product）
- |A| 是向量 A 的模长（magnitude）
- |B| 是向量 B 的模长
```

### 2.2 具体计算例子

**例子 1：完全相同的向量**

```
向量 A: [1, 0, 0]
向量 B: [1, 0, 0]

点积：  A · B = 1×1 + 0×0 + 0×0 = 1
模长：  |A| = √(1² + 0² + 0²) = 1
模长：  |B| = √(1² + 0² + 0²) = 1

相似度 = 1 / (1 × 1) = 1.0  ✓ 完全相同
```

**例子 2：完全不同的向量**

```
向量 A: [1, 0, 0]
向量 C: [0, 1, 0]

点积：  A · C = 1×0 + 0×1 + 0×0 = 0
模长：  |A| = 1
模长：  |C| = 1

相似度 = 0 / (1 × 1) = 0.0  ✓ 完全不同
```

**例子 3：部分相似的向量**

```
向量 A: [1, 0, 0]
向量 D: [0.7, 0.7, 0]

点积：  A · D = 1×0.7 + 0×0.7 + 0×0 = 0.7
模长：  |A| = √(1² + 0² + 0²) = 1
模长：  |D| = √(0.7² + 0.7² + 0²) = √0.98 ≈ 0.99

相似度 = 0.7 / (1 × 0.99) ≈ 0.707  ✓ 部分相似
```

### 2.3 为什么使用余弦相似度？

**优点 1：方向性而非距离**

```
向量 A: [1, 0, 0]
向量 B: [2, 0, 0]  （A 的 2 倍）

欧几里得距离：√((2-1)² + 0² + 0²) = 1  （有差异）
余弦相似度：  1.0  （完全相同方向）

在 RAG 中，我们关心的是"方向"（含义），而不是"长度"（强度）
所以余弦相似度更合适
```

**优点 2：结果范围固定**

```
余弦相似度范围：-1 到 1（通常 0 到 1）
- 1.0：完全相同
- 0.5：中等相似
- 0.0：完全不同
```

### 2.4 相似度阈值的实际意义

**在 RAG 中的应用：**

```
相似度 > 0.9：
  - 文本几乎相同
  - 例如："退货政策" vs "退货规则"
  - 用途：精确匹配

相似度 0.7-0.9：
  - 文本同一主题
  - 例如："退货政策" vs "如何退货"
  - 用途：相关文档检索（推荐）

相似度 0.5-0.7：
  - 文本有关联但不同主题
  - 例如："退货政策" vs "退款流程"
  - 用途：可能相关，需要谨慎

相似度 < 0.5：
  - 文本完全不同
  - 例如："退货政策" vs "产品介绍"
  - 用途：不相关，应该过滤
```

**实际选择：**
```
高精度场景（金融、医疗）：阈值 = 0.8-0.9
平衡场景（客服、搜索）：阈值 = 0.7
高召回场景（信息检索）：阈值 = 0.5
```

---

## 第三章：RAG 提示词的构建

### 3.1 普通提示词 vs RAG 提示词

**普通提示词：**

```
系统提示词：你是一个专业的客服

用户问题：退货要多少钱？

LLM 的回答：
基于我的训练数据，通常退货费用...
（可能过时或错误）
```

**RAG 提示词：**

```
系统提示词：
你是一个专业的客服。
请基于以下信息回答用户的问题。
如果信息中没有相关内容，请说"我不知道"。

检索到的信息：
1. 退货费用：首次退货免费，之后每次 10 元
2. 退货时间：7 天内无理由退货
3. 退货流程：联系客服 → 获取退货单 → 寄回 → 确认收货 → 退款

用户问题：退货要多少钱？

LLM 的回答：
根据我们的政策，首次退货是免费的，第二次及以后每次需要 10 元。
（基于检索到的信息，准确可靠）
```

### 3.2 RAG 提示词的三个关键部分

**部分 1：系统角色定义**

```
你是一个专业的客服。
你的职责是：
- 基于提供的信息回答问题
- 如果信息不足，说"我不知道"
- 不要编造信息
- 保持友好和专业的语气
```

**部分 2：检索到的信息**

```
基于以下信息回答问题：

[信息 1]
[信息 2]
[信息 3]
...

注意：
- 信息按相似度排序（最相关的在前）
- 通常包含 3-5 个最相关的文档片段
- 每个片段应该是完整的句子或段落
```

**部分 3：用户问题**

```
用户问题：{query}

请基于上述信息回答这个问题。
```

### 3.3 提示词构建的最佳实践

**实践 1：明确指示 LLM 的行为**

```
不好的做法：
"回答这个问题"

好的做法：
"基于以下信息回答这个问题。
如果信息中没有相关内容，请说'我不知道'。
不要编造或推测信息。"
```

**实践 2：清晰地分隔信息和问题**

```
不好的做法：
系统提示词：你是客服
信息和问题混在一起

好的做法：
系统提示词：[明确定义]
---
检索到的信息：[清晰标记]
---
用户问题：[清晰标记]
```

**实践 3：控制信息量**

```
太少信息（< 100 字符）：
- LLM 可能无法回答
- 相关性可能不足

太多信息（> 2000 字符）：
- Token 消耗过多
- LLM 可能混淆
- 成本增加

推荐：300-1000 字符（3-5 个文档片段）
```

---

## 第四章：RAG 的完整流程

### 4.1 文档加载和分块

**为什么需要分块？**

```
原始文档：
"退货政策

1. 退货时间：自收货之日起 7 天内
2. 退货条件：商品未使用，包装完整
3. 退货流程：
   - 联系客服获取退货单号
   - 将商品寄回（运费自理）
   - 我们确认收货后进行退款
4. 退货费用：
   - 首次退货：免费
   - 第二次及以后：每次 10 元
5. 退款时间：确认收货后 3-5 个工作日"

问题：
- 整个文档太长（可能 > 2000 tokens）
- 向量化整个文档会丢失细节
- 检索时无法精确定位相关部分

解决方案：分块处理
```

**分块策略：**

```
块大小：512 字符
重叠：50 字符

块 1：
"退货政策

1. 退货时间：自收货之日起 7 天内
2. 退货条件：商品未使用，包装完整
3. 退货流程：
   - 联系客服获取退货单号
   - 将商品寄回（运费自理）"

块 2：
"   - 将商品寄回（运费自理）
   - 我们确认收货后进行退款
4. 退货费用：
   - 首次退货：免费
   - 第二次及以后：每次 10 元
5. 退款时间：确认收货后 3-5 个工作日"

优点：
- 每个块都是完整的信息单元
- 重叠部分保证上下文连贯
- 向量化更精确
```

### 4.2 完整的 RAG 流程图

```
用户问题：退货要多少钱？
    ↓
[步骤 1] 向量化查询
    ↓
查询向量：[0.245, -0.578, 0.123, ..., 0.456]
    ↓
[步骤 2] 检索相似文档
    ↓
块 1：相似度 0.92 - "退货费用：首次退货免费，之后 10 元"
块 2：相似度 0.78 - "退货时间：7 天内"
块 3：相似度 0.65 - "退货流程：联系客服..."
    ↓
[步骤 3] 过滤（相似度 > 0.7）
    ↓
块 1：相似度 0.92 ✓
块 2：相似度 0.78 ✓
块 3：相似度 0.65 ✗ （过滤掉）
    ↓
[步骤 4] 构建 RAG 提示词
    ↓
系统提示词：你是客服，基于以下信息回答问题
检索信息：
- 退货费用：首次退货免费，之后 10 元
- 退货时间：7 天内
用户问题：退货要多少钱？
    ↓
[步骤 5] 调用 LLM
    ↓
LLM 回答：根据我们的政策，首次退货是免费的，
         第二次及以后每次需要 10 元。
    ↓
[步骤 6] 返回给用户
```

### 4.3 RAG 的关键决策点

**决策 1：检索多少个文档？**

```
检索 1 个：
- 速度快
- 可能信息不足
- 推荐用于：简单问题

检索 3-5 个：
- 平衡速度和准确性
- 推荐用于：大多数场景

检索 10+ 个：
- 信息全面
- Token 消耗多
- 推荐用于：复杂问题
```

**决策 2：相似度阈值设置**

```
高阈值（0.8-0.9）：
- 只返回最相关的文档
- 准确性高，但可能漏掉相关信息
- 推荐用于：金融、医疗等高精度场景

中阈值（0.7）：
- 平衡准确性和召回率
- 推荐用于：客服、搜索等通用场景

低阈值（0.5）：
- 返回所有可能相关的文档
- 召回率高，但可能包含噪音
- 推荐用于：信息检索、研究
```

**决策 3：如何处理无关问题？**

```
问题：你们的产品质量怎么样？
检索结果：相似度都 < 0.5（无相关文档）

选项 1：返回最相关的文档
- 风险：可能给出错误答案

选项 2：说"我不知道"
- 安全：避免幻觉
- 推荐做法

实现方式：
if (maxSimilarity < 0.5) {
    return "抱歉，我在文档中没有找到相关信息。";
}
```

---

## 第五章：RAG 的常见陷阱

### 5.1 陷阱 1：幻觉（Hallucination）

**问题：** LLM 编造信息

```
文档中没有的信息：
"我们的退货政策是业界最好的"

用户问题：你们的退货政策怎么样？

不好的回答（幻觉）：
"我们的退货政策是业界最好的，
 提供 30 天无条件退货..."

好的回答（基于 RAG）：
"根据我们的政策，
 7 天内无理由退货，
 首次免费，之后每次 10 元。"
```

**防止方法：**
```java
// 在提示词中明确指示
"如果信息中没有相关内容，请说'我不知道'。
 不要编造或推测信息。"

// 检查相似度
if (maxSimilarity < 0.5) {
    return "我不知道";
}
```

### 5.2 陷阱 2：信息过时

**问题：** 检索到的信息已经过时

```
文档中的信息：
"退货费用：首次免费，之后 10 元"
（写于 2024 年）

现在是 2026 年，政策已改为：
"退货费用：全部免费"

但系统仍然返回旧信息
```

**防止方法：**
```
1. 定期更新文档
2. 在文档中标记时间戳
3. 在提示词中说明信息的时效性
4. 定期审查和更新知识库
```

### 5.3 陷阱 3：上下文混淆

**问题：** 检索到的文档片段缺乏上下文

```
文档原文：
"退货费用：首次退货免费。
 但如果商品有损坏，需要收取 50 元的检测费。"

检索到的片段：
"首次退货免费"

LLM 的回答：
"首次退货完全免费"
（忽略了损坏需要检测费的条件）
```

**防止方法：**
```
1. 增加文档片段的重叠
2. 在分块时保留上下文
3. 检索时返回更多上下文
4. 在提示词中强调条件和例外
```

---

## 第六章：RAG 的性能优化

### 6.1 优化 1：缓存

**问题：** 相同的查询重复调用 LLM

```
用户 1：退货要多少钱？ → 调用 LLM → 成本 $0.001
用户 2：退货要多少钱？ → 调用 LLM → 成本 $0.001
用户 3：退货要多少钱？ → 调用 LLM → 成本 $0.001

总成本：$0.003（浪费）
```

**解决方案：缓存**

```
用户 1：退货要多少钱？ → 调用 LLM → 缓存结果 → 成本 $0.001
用户 2：退货要多少钱？ → 从缓存返回 → 成本 $0
用户 3：退货要多少钱？ → 从缓存返回 → 成本 $0

总成本：$0.001（节省 66%）
```

### 6.2 优化 2：向量化缓存

**问题：** 相同的文档重复向量化

```
文档加载时：
块 1 → 向量化 → 存储
块 2 → 向量化 → 存储
块 3 → 向量化 → 存储

查询时：
查询 → 向量化 → 检索

如果查询相同，向量化结果应该相同
```

**解决方案：缓存查询向量**

```
查询 1：退货要多少钱？ → 向量化 → 缓存 → 检索
查询 2：退货要多少钱？ → 从缓存获取向量 → 检索

节省向量化时间
```

### 6.3 优化 3：异步处理

**问题：** 用户等待向量化和检索完成

```
同步流程：
用户请求 → 向量化（200ms）→ 检索（50ms）→ LLM 调用（2000ms）
总耗时：2250ms（用户等待）
```

**解决方案：异步处理**

```
异步流程：
用户请求 → 立即返回 "处理中..."
后台：向量化 → 检索 → LLM 调用 → 通知用户

用户体验更好
```

---

## 总结：RAG 的核心要点

1. **向量化：** 将文本转换为数字向量，使计算机能理解含义
2. **相似度：** 使用余弦相似度计算向量的接近程度
3. **检索：** 找到与查询最相似的文档片段
4. **提示词：** 将检索到的信息和用户问题组合成提示词
5. **LLM：** 基于提示词生成答案

**RAG 的优势：**
- 避免幻觉（基于真实文档）
- 支持最新信息（可更新知识库）
- 可追溯（知道答案来自哪个文档）
- 成本低（不需要微调 LLM）

**RAG 的挑战：**
- 检索质量影响最终答案
- 需要维护知识库
- 向量化和检索有延迟
- 需要调整相似度阈值

---

## 下一步

理解了这些概念后，你可以：
1. 启动 Scenario-2 进行实战练习
2. 观察向量化和检索的实际过程
3. 调整参数看效果变化
4. 理解为什么某些查询返回特定结果
